#include<iostream>
#include<vector>
#include<list>
#include<string>
#include<algorithm>
#include<stack>
#include<cmath>
using namespace std;

int main()
{
	int t;
	vector <int> answer;
	while(t>0)
	{
		int n;
		cin >> n;
		if(n == 0)
		{
			break;
		}
		vector <int> arr;
		for(int i=0;i<n;i++)
		{
			int temp;
			cin >> temp;
			arr.push_back(temp);
		}
				
	}
	return 0;
}
