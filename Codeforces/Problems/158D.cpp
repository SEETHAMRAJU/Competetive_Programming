#include<bits/stdc++.h>
#define append push_back
#define sint(n) scanf("%d",&n)
#define f(i,n) for(int i=0;i<n;i++)
using namespace std;

int main()
{
	int n;
	scanf("%d",&n);
	f(i,n)
	{
		sint(temp);
		q.append(temp);
		if(temp<0)
			arr[temp]=1;
	}
	
	return 0;
}





