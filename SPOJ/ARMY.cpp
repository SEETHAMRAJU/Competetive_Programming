#include<bits/stdc++.h>
#define append push_back
using namespace std;
int main()
{
	int t;
	vector <int> answer;	
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
		int G,M;
		scanf("%d %d",&G,&M);
		vector <int> g,m;		
		for(int i=0;i<g;i++)
		{
			scanf("%d",&temp);
			g.append(temp);			
		}
		for(int i=0;i<m;i++)
		{
			scanf("%d",&temp);
			m.append(temp);			
		}
		sort(q.begin(),q.end());
		sort(m.begin(),m.end());
		//Solving who wins!!		
		int i,j;
		i = 0;
		j = 0;
		while(i<G && j<M)
		{
			if()
		}

		

	}
	return 0;
}
